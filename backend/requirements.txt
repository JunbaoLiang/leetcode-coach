# Deployment manifest (Railway/Render auto-detect this and wire up a venv +
# PATH correctly). Keep in sync with [project.dependencies] in pyproject.toml.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
alembic>=1.13
anthropic>=0.40
httpx>=0.27
psycopg[binary]>=3.1
